node_modules/

# Cached source data (bhavcopy CSVs, etc.)
.cache/

# Local secrets
.env
.env.*
!.env.example

# Logs / debug
*.log
npm-debug.log*

# Playwright browser binaries live in ~/.cache by default; ignore any local copies
/playwright-browsers/

# Debug HTML dumps (uploaded as a CI artifact, never committed)
public/data/debug/
